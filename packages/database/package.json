{
  "name": "@ui-quality/database",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"const fs=require('fs');const p='dist/migrations';fs.mkdirSync(p,{recursive:true});for(const f of fs.readdirSync('migrations').filter(x=>x.endsWith('.sql'))) fs.copyFileSync('migrations/'+f,p+'/'+f);\"",
    "migrate": "tsx src/migrate.ts",
    "backup": "tsx src/maintenance/backup.ts",
    "restore": "tsx src/maintenance/restore.ts"
  },
  "dependencies": {
    "pg": "^8.13.0",
    "@ui-quality/storage": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.10",
    "tsx": "^4.16.5"
  }
}
